<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reservationapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/reservapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_reserv"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityReservationAdd"
            android:launchMode="singleTask"/>
        <activity android:name=".ActivityTransitInfo"
            android:launchMode="singleTask"/>
        <activity android:name=".ActivityTransitList"
            android:launchMode="singleTask"/>
        <activity android:name=".NavigationActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ShuttleActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".AccountRegisterActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".AccountLoginActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyACKj8z0tkOsTpJqmg529lfxmulGPnRVf0" />
    </application>

</manifest>